<!DOCTYPE busconfig PUBLIC
 "-//freedesktop//DTD D-Bus Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">

<!--
  System-bus access policy for dev.sniner.Revenant.

  Installed to: /usr/share/dbus-1/system.d/dev.sniner.Revenant.conf

  Owning the bus name is restricted to root (the daemon itself);
  client side, any user may call methods and receive signals.
  Authorization for individual actions is delegated to polkit
  (see dev.sniner.Revenant.policy).
-->
<busconfig>
  <policy user="root">
    <allow own="dev.sniner.Revenant"/>
    <allow send_destination="dev.sniner.Revenant"/>
    <allow receive_sender="dev.sniner.Revenant"/>
  </policy>

  <policy context="default">
    <allow send_destination="dev.sniner.Revenant"/>
    <allow receive_sender="dev.sniner.Revenant"/>
  </policy>
</busconfig>
